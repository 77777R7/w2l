# Phase 4 Installation Validation

Status: `not_run`.

Required protocol for A6:

1. A developer other than the author clones the repository at the release/main commit.
2. Runs `npm ci`.
3. Installs Chromium with the documented command.
4. Starts the local API.
5. Runs one task from `research/phase4_real_tasks.json`.
6. Records command output, commit, OS, Node, browser version, elapsed time, and any manual intervention.

This file is intentionally a gate record, not a claim that the protocol has passed.
