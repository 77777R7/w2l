/**
 * Single-sourced UA values live in @w2l/contracts (modeIdentity / RESEARCH_USER_AGENT).
 * This file is the historical home of the polite UA and is kept as a re-export
 * so existing subjects don't churn; new code should import from @w2l/contracts.
 */

export { RESEARCH_USER_AGENT as POLITE_UA } from '@w2l/contracts'
