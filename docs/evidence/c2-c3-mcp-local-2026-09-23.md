# C2/C3 local MCP first-use evidence — 2026-09-23

Implementation source: `fcacf286698f88d231f622166fa78a9e06282b6a`
on `codex/c2-c3-remote-mcp`. The verified source files match this commit;
the detailed runtime database and receiver log remain in ignored local
`.w2l/c2-first-use-1790142167255/` and are not distributed.

The check ran on Node `v26.8.1` and npm `11.19.0`. `npm run typecheck`
passed. The full suite passed **87 test files, 984 tests**. The independent
HTTPS first-use run completed at `2026-09-23T05:44:03.136Z` with a real
capture of `https://docs.firecrawl.dev/introduction` and a temporary
Cloudflare HTTPS tunnel to the sample receiver. The tunnel was stopped.

| Check | Observed result |
| --- | --- |
| MCP transport | Official SDK Streamable HTTP client connected and called tools |
| Source sample | `valid`, with field evidence |
| Initial event | `cd6d23ad-f94b-4c83-994a-8508f94045c6` |
| Pending delivery across `SIGKILL` | Delivery `788c04a0-784a-4cbd-9ded-4f23a907437f` remained pending, then delivered after process restart |
| Receiver | One durable receipt and projection for the same event ID |
| Queued run across `SIGKILL` | Run `c1779c83-d29d-49b4-848f-2dc045ab7dff` completed after process restart as `unchanged` |
| End state | Monitor paused |

The evidence client used a **local test token verifier**. Production code
uses WorkOS JWKS, exact issuer/audience, owner `sub`, expiry and scope checks,
but this experiment did not run browser OAuth or authenticate a real Codex
client. A separate protocol test rejected missing tokens, invalid Origin,
another user, missing scope, unsupported protocol versions, and forbidden
source/receiver inputs. The real hosted connection, hosted task completion
and hosted restart/continued-monitoring gates therefore remain **open**.

At review time the authenticated Render dashboard reported the workspace
**suspended due to non-payment**. A fresh `render login` succeeded. The
authenticated `render blueprints validate -o json` first found two unsupported
shutdown-delay settings on disk-backed services; those were removed. Its
remaining errors are `billing_suspended` on the workspace and both services.
No WorkOS AuthKit project/resource/owner subject had been provided. Neither
Render service was created or deployed, and there has been no live Docker
build on Render.
